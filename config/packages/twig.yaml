twig:
    default_path: '%kernel.project_dir%/templates'
    form_themes: ['bootstrap_5_layout.html.twig']
    globals:
        google_analytics_id: '%env(GOOGLE_ANALYTICS_ID)%'
        notifications: '@App\Util\NotificationService'

when@test:
    twig:
        strict_variables: true
